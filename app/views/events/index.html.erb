<h2>Past Events:</h2>
<ul>
  <% @events.past.each do |event| %>
    <li><%= link_to event.title, event %></li>
  <% end %>
</ul>

<h2>Future Events:</h2>
<ul>
  <% @events.future.each do |event| %>
    <li><%= link_to event.title, event %></li>
  <% end %>
</ul>